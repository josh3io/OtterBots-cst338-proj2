<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.OtterBotsRockPaperScissors">
    <activity
      android:exported="false"
      android:name=".ResetStats" />
    <activity
      android:exported="false"
      android:name=".UpdateUserDetails" />
    <activity
      android:exported="false"
      android:name=".AdminNewUserActivity" />
    <activity
      android:exported="false"
      android:name=".AdminDeleteUserActivity" />
    <activity
      android:exported="false"
      android:name=".GamePlayActivity" />
    <activity
      android:exported="false"
      android:name=".RegisterActivity" />
    <activity
      android:exported="false"
      android:name=".LeaderboardActivity" />
    <activity
      android:exported="false"
      android:name=".LandingActivity" />
    <activity
      android:exported="true"
      android:name=".LoginActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
  <uses-permission android:name="android.permission.INTERNET" />

  <uses-permission android:name="android.permission.INTERNET" />

</manifest>